#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t,n,i;
    while(cin>>t)
    {
        int a[n];
        std::string s;
        for(i=0;i<t;i++)
        {
            cin>>a[i];
            string.at(s);
        }
    }
}
